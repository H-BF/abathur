{
  "name": "reporter",
  "version": "1.0.0",
  "description": "",
  "main": "./build/index.js",
  "scripts": {
    "build": "tsc",
    "rebuild": "rimraf ./build && tsc",
    "start:prod": "node ./build/index.js",
    "start:reporter": "rimraf ./build && tsc && node ./build/index.js",
    "proto:gen": "proto-loader-gen-types --grpcLib=@grpc/grpc-js --outDir=./gRPC ./gRPC/reporter.proto"
  },
  "author": "Ari Insomnia",
  "license": "ISC",
  "devDependencies": {
    "tslib": "^2.6.0",
    "typescript": "^5.1.6"
  },
  "dependencies": {
    "@grpc/grpc-js": "^1.8.17",
    "@grpc/proto-loader": "^0.7.7",
    "rimraf": "^5.0.1"
  }
}
